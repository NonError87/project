# -*- coding: utf-8 -*-

import os

for command in [
    "pip install beautifulsoup4",
    "pip install html5lib"
    "pip install utils",
    "pip install db",
    "pip install request",
    "pip install http",
    "pip install webapi",
    "pip install httpserver"
    "easy_install web.py"
]:
    os.system(command)
